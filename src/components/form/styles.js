import styled from 'styled-components';
import tw from 'tailwind.macro';

export const Form = styled.form`
  ${tw`flex flex-col items-center justify-center mx-auto`};
`;

export const Feedback = styled.span`
  ${tw`mt-5`};
`;
